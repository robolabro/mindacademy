[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
    "python manage.py collectstatic --no-input",
    "python manage.py migrate --no-input"
]

[start]
cmd = "gunicorn mathcourses.wsgi --log-file -"
